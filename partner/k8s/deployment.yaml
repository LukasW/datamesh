apiVersion: v1
kind: Service
metadata:
  name: partner-service
  namespace: datamesh
spec:
  selector:
    app: partner-service
  ports:
    - port: 9080
      targetPort: 9080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: partner-service
  namespace: datamesh
spec:
  replicas: 1
  selector:
    matchLabels:
      app: partner-service
  template:
    metadata:
      labels:
        app: partner-service
    spec:
      containers:
        - name: partner-service
          image: yuno/partner-service:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: QUARKUS_PROFILE
              value: prod
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: kafka:29092
            - name: DATABASE_URL
              value: jdbc:postgresql://partner-db:5432/partner_db
            - name: DATABASE_USER
              value: partner_user
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-passwords
                  key: partner-db-password
            - name: KEYCLOAK_URL
              value: http://keycloak:8080
            - name: KEYCLOAK_FRONTEND_URL
              value: http://keycloak.localhost
            - name: OIDC_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: keycloak-config
                  key: oidc-client-secret
            - name: VAULT_URL
              value: http://vault:8200
            - name: VAULT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: vault-config
                  key: vault-token
            - name: QUARKUS_OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://jaeger:4317
          ports:
            - containerPort: 9080
          readinessProbe:
            httpGet:
              path: /q/metrics
              port: 9080
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            limits:
              memory: 384Mi
