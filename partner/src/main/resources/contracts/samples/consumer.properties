# Sample consumer configuration for Partner domain events
# Replace placeholders with actual values
bootstrap.servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
group.id=my-consumer-group
auto.offset.reset=earliest
key.deserializer=org.apache.kafka.common.serialization.StringDeserializer
value.deserializer=org.apache.kafka.common.serialization.StringDeserializer
# Subscribe to one or more Partner topics:
# person.v1.created, person.v1.updated, person.v1.deleted
# person.v1.address-added, person.v1.address-updated
# person.v1.state (compacted – for bootstrapping)
