{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PersonDeleted",
  "description": "Emitted when a person is soft-deleted from the partner domain.",
  "type": "object",
  "required": [
    "eventId",
    "eventType",
    "personId",
    "timestamp"
  ],
  "additionalProperties": false,
  "properties": {
    "eventId": {
      "type": "string",
      "format": "uuid",
      "description": "Unique identifier for this event instance."
    },
    "eventType": {
      "type": "string",
      "enum": ["PersonDeleted"],
      "description": "Discriminator for the event type."
    },
    "personId": {
      "type": "string",
      "format": "uuid",
      "description": "Unique identifier of the person aggregate."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 timestamp of when the event was produced."
    }
  }
}
