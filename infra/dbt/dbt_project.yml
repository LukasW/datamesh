name: platform
version: 1.0.0
config-version: 2

profile: platform

model-paths: [models]
target-path: target
clean-targets: [target, dbt_packages]

models:
  platform:
    staging:
      +materialized: view
    marts:
      +materialized: table
