{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ClaimSettled",
  "description": "Event emitted when an insurance claim is settled.",
  "type": "object",
  "required": [
    "eventId",
    "eventType",
    "claimId",
    "claimNumber",
    "policyId",
    "claimDate",
    "status",
    "settlementAmount",
    "timestamp"
  ],
  "additionalProperties": false,
  "properties": {
    "eventId": {
      "type": "string",
      "format": "uuid",
      "description": "Unique identifier for this event."
    },
    "eventType": {
      "type": "string",
      "enum": ["ClaimSettled"],
      "description": "The type of domain event."
    },
    "claimId": {
      "type": "string",
      "format": "uuid",
      "description": "Unique identifier of the claim."
    },
    "claimNumber": {
      "type": "string",
      "description": "Human-readable claim number."
    },
    "policyId": {
      "type": "string",
      "format": "uuid",
      "description": "Identifier of the associated policy."
    },
    "claimDate": {
      "type": "string",
      "format": "date",
      "description": "Date when the insured event occurred (ISO 8601)."
    },
    "status": {
      "type": "string",
      "description": "Current status of the claim."
    },
    "settlementAmount": {
      "type": "number",
      "description": "Amount paid out for the claim (CHF)."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when the event was created (ISO 8601)."
    }
  }
}
