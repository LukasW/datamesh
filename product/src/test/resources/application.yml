quarkus:
  http:
    port: 0
    test-port: 0

  datasource:
    db-kind: postgresql
    devservices:
      enabled: true

  flyway:
    migrate-at-start: true

  oidc:
    enabled: false

  security:
    auth:
      enabled-in-dev-mode: false

  log:
    level: INFO
