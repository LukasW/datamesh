# Disable all Kafka messaging channels for integration tests (DB-only tests)
# These override the main application.properties Kafka channel configuration
mp.messaging.incoming.partner-person-created.enabled=false
mp.messaging.incoming.partner-person-updated.enabled=false
mp.messaging.incoming.product-defined-in.enabled=false
mp.messaging.incoming.product-updated-in.enabled=false
mp.messaging.incoming.product-deprecated-in.enabled=false
